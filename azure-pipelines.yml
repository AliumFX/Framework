trigger:
  branches:
    include:
<<<<<<< HEAD
    - develop
    - master
    - features/*
  paths:
    exclude:
    - .editorconfig
    - LICENSE
    - README.md
    - appveyor.yml
    - .travis.yml
    - .circleci/config.yml
    
=======
      - develop
      - master
      - features/*
  paths:
    exclude:
      - .editorconfig
      - LICENSE
      - README.md
      - appveyor.yml
      - .travis.yml
      - .circleci/config.yml

>>>>>>> develop
pool:
  vmImage: 'vs2017-win2016'

variables:
  buildConfiguration: 'Release'
<<<<<<< HEAD
  
steps:
- script: git submodule init
- script: git submodule update

- task: PowerShell@2
  inputs:
    filePath: 'install.ps1'
    pwsh: true
  displayName: Install .NET Core

- task: PowerShell@2
  inputs:
    filePath: 'build.ps1'
    pwsh: true
  displayName: Build Alium Framework

- task: PublishTestResults@2
  inputs:
    testRunner: VSTest
    testResultsFiles: 'build/testResults/**/*.xml'

=======

steps:
  - script: git submodule init
  - script: git submodule update

  - task: PowerShell@2
    inputs:
      filePath: 'install.ps1'
      pwsh: true
    displayName: Install .NET Core

  - task: PowerShell@2
    inputs:
      filePath: 'build.ps1'
      pwsh: true
    displayName: Build Alium Framework

  - task: PublishTestResults@2
    inputs:
      testRunner: VSTest
      testResultsFiles: 'build/test-results/**/*.xml'
>>>>>>> develop
